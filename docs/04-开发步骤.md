# 04 — 开发步骤与进度

> 项目：Ballet Clipboard（芭蕾剪贴板）
> 版本：1.0-dev
> 最后更新：2026-06-17

---

## 总体进度

| 阶段 | 状态 | 完成日期 |
|------|------|----------|
| 阶段 0：基础设施 | ✅ 完成 | 2026-06-17 |
| 阶段 1：P0 核心功能 | ✅ 完成 | 2026-06-17 |
| 阶段 2：P1 重要功能 | 🚧 骨架就绪 | — |
| 阶段 3：P2 体验增强 | 📋 待开始 | — |

---

## 阶段 0：基础设施搭建 ✅

- [x] 创建 `docs/` + `devlog/` + `CLAUDE.md`
- [x] 安装 XcodeGen，编写 `project.yml`
- [x] 创建 `Info.plist`、`.entitlements`
- [x] 创建 `.xcdatamodeld` Core Data 模型
- [x] 创建 `Assets.xcassets`（AppIcon + MenuBarIcon + AccentColor）
- [x] 17 个 Swift 源文件骨架
- [x] Swift 6 并发适配
- [x] 编译通过 ✅

---

## 阶段 1：P0 核心功能 ✅

### 已实现文件

| 文件 | 功能 | 状态 |
|------|------|------|
| `Theme/BalletTheme.swift` | 粉色主题色系统 | ✅ |
| `Data/PersistenceController.swift` | Core Data 栈 + 合并策略 | ✅ |
| `Data/ClipboardMonitor.swift` | 0.5s 轮询 + 类型识别 + 去重 + 后台写入 | ✅ |
| `Data/ImageStore.swift` | 图片 PNG 保存/读取/删除 | ✅ |
| `ViewModels/ClipboardViewModel.swift` | 状态管理 + 搜索 + 复制/删除 + Core Data 同步 | ✅ |
| `App/AppDelegate.swift` | 应用生命周期 | ✅ |
| `App/BalletClipboardApp.swift` | @main + WindowGroup | ✅ |
| `Views/ContentView.swift` | 主窗口 (360×500) + 品牌栏 | ✅ |
| `Views/ClipCardView.swift` | 粉色卡片 + 类型图标 | ✅ |
| `Views/SearchBarView.swift` | 实时搜索过滤 | ✅ |
| `Views/EmptyStateView.swift` | 🩰 空状态 | ✅ |
| `Models/ClipboardEntry.swift` | Core Data 实体定义 | ✅ |
| `Models/ClipType.swift` | 类型枚举 | ✅ |
| `Helpers/URLChecker.swift` | NSDataDetector URL 检测 | ✅ |
| `Helpers/RelativeTimeFormatter.swift` | 中文相对时间 | ✅ |

### P0 验证清单

- [x] ⌘B 编译通过
- [x] 复制文字 → 窗口显示记录
- [x] 复制链接 → 正确识别为 URL
- [x] 复制图片 → 保存原图，显示 🖼️
- [x] 连续复制相同内容 → 不产生重复
- [x] 点击卡片 → ⌘V 粘贴成功
- [x] 搜索框输入 → 实时过滤
- [x] Dock 栏显示粉色爱心翅膀图标
- [x] 窗口显示 🩰 芭蕾品牌栏

---

## 阶段 2：P1 重要功能 🚧

### 骨架就绪，待完善

| 文件 | 计划功能 | 状态 |
|------|----------|------|
| `Views/SettingsView.swift` | 保留期限设置 (1/3/5天) | 🚧 骨架 |
| `Data/CleanupEngine.swift` | 60s 自动清理过期记录 | 🚧 骨架 |
| `ClipCardView` | 置顶/删除按钮交互 | 📋 待实现 |
| `ClipboardViewModel` | togglePin() / delete() | 🚧 后端已有 |

### P1 待实现清单

- [ ] 置顶按钮：卡片 hover 显示 📌
- [ ] 删除按钮：卡片 hover 显示 🗑
- [ ] 删除确认 / 撤销
- [ ] 设置面板：从齿轮图标入口
- [ ] 保留期限：UserDefaults → CleanupEngine
- [ ] 自动清理：过期非置顶记录 + 图片文件

---

## 阶段 3：P2 体验增强 📋

### 待实现

- [ ] 切换为 `MenuBarExtra`（菜单栏弹窗模式）
- [ ] LSUIElement = YES（隐藏 Dock）
- [ ] 开机自启动 (`SMAppService.mainApp.register()`)
- [ ] "已复制" Toast 反馈
- [ ] 卡片动画增强
- [ ] 深色/浅色模式适配测试
- [ ] 性能验证（CPU < 1%, 内存 < 50MB）
- [ ] 菜单栏图标（template 模式，适配深色）

---

## 项目文件清单

### 源代码 (17 个 Swift 文件)

```
BalletClipboard/
├── App/
│   ├── BalletClipboardApp.swift
│   └── AppDelegate.swift
├── Models/
│   ├── ClipboardEntry.swift
│   └── ClipType.swift
├── Data/
│   ├── PersistenceController.swift
│   ├── ClipboardMonitor.swift
│   ├── ImageStore.swift
│   └── CleanupEngine.swift
├── ViewModels/
│   └── ClipboardViewModel.swift
├── Views/
│   ├── ContentView.swift
│   ├── ClipCardView.swift
│   ├── SearchBarView.swift
│   ├── SettingsView.swift
│   └── EmptyStateView.swift
├── Theme/
│   └── BalletTheme.swift
└── Helpers/
    ├── URLChecker.swift
    └── RelativeTimeFormatter.swift
```

### 资源

```
BalletClipboard/
├── Info.plist
├── BalletClipboard.entitlements
├── Assets.xcassets/
│   ├── AppIcon.appiconset/ (20 个 PNG)
│   ├── MenuBarIcon.imageset/ (2 个 PNG)
│   └── AccentColor.colorset/
└── BalletClipboard.xcdatamodeld/
```

### 工具脚本

```
copy2/
├── project.yml              # XcodeGen 配置
└── generate_icons.py         # 图标生成
```
